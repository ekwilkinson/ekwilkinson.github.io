domain: ekwilkinson.github.io       # if you want to force HTTPS, specify the domain without the http at the start, e.g. example.com
url: https://ekwilkinson.github.io  # the base hostname and protocol for your site, e.g. http://example.com
baseurl: home      # place folder name if the site is served in a subfolderbaseurl: 'home'